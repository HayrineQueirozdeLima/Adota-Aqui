/**
 * DTOs: os formatos de entrada e saída da API.
 *
 * A entidade nunca vai direto pra resposta. Primeiro porque a senha não pode sair de jeito nenhum,
 * segundo porque o JSON que o front espera nem sempre é igual à tabela.
 *
 * Os campos de cada DTO seguem o que está no docs/api.md.
 * Sugestão de nome: CadastroUsuarioRequest, AnimalResponse, LoginResponse...
 */
package com.adotaaqui.dto;
