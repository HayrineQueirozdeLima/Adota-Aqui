/**
 * Controllers: as rotas da API (ex.: POST /api/animais).
 *
 * O controller só recebe a requisição, chama o service e devolve a resposta.
 * Nada de regra de negócio aqui. Se bateu a dúvida "isso é regra?", provavelmente é, e vai pro service.
 *
 * Cada endpoint precisa estar igualzinho ao docs/api.md (URL, método, JSON e códigos de erro).
 * Mudou alguma coisa? Atualiza o api.md no mesmo PR.
 */
package com.adotaaqui.controller;
