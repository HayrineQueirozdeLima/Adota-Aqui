package com.adotaaqui.model.enums;

// Essa é a única pergunta da triagem sem a opção "prefiro responder ao protetor".
// O contato é o que faz a ponte entre adotante e protetor, então é obrigatório (RF15).
public enum MomentoContato {
    MANHA,
    TARDE,
    NOITE,
    QUALQUER_HORARIO
}
